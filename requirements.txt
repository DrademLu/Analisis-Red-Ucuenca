# Dependencias del pipeline — red_ucuenca_redes_complejas
#
# IMPORTANTE: este archivo usa rangos conservadores. Para la entrega final,
# REEMPLAZALO por la salida exacta de tu entorno actual:
#
#     pip freeze > requirements.txt
#
# Los rangos de abajo sirven solo como red de seguridad si el freeze no se hace.
#
# ---------------------------------------------------------------------------
# numpy < 2.0 es OBLIGATORIO mientras 08 y 11 usen np.trapz:
# esa funcion fue eliminada en numpy 2.0 (renombrada a np.trapezoid).
# Si aplicas el parche de np.trapezoid, puedes relajar el limite superior.
numpy>=1.24,<2.0

pandas>=1.5,<3.0
networkx>=3.0,<3.5
matplotlib>=3.6,<4.0

# scipy.optimize.milp (usado en 07_p7_localizacion.py) requiere scipy >= 1.9
scipy>=1.9,<2.0

# normalized_mutual_info_score / adjusted_rand_score (04_p4_comunidades.py)
scikit-learn>=1.2,<2.0
